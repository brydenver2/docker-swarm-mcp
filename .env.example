# Docker Connection Configuration
# Default: Local Docker socket (unix:///var/run/docker.sock)
DOCKER_HOST=unix:///var/run/docker.sock

# For remote Docker via TLS:
# DOCKER_HOST=tcp://remote-host:2376
# DOCKER_TLS_VERIFY=1
# DOCKER_CERT_PATH=/path/to/certs

# For remote Docker via SSH:
# DOCKER_HOST=ssh://user@remote-host

# TLS Configuration (only needed for tcp:// connections)
DOCKER_TLS_VERIFY=0
DOCKER_CERT_PATH=

# MCP Server Configuration
# SECURITY: Either MCP_ACCESS_TOKEN or TOKEN_SCOPES MUST be set
# REQUIRED: Generate a secure random token for production
# Example: openssl rand -hex 32
MCP_ACCESS_TOKEN=your-secure-token-here

# Multi-token authentication with per-token scopes (optional)
# JSON mapping: {"token1": ["admin"], "token2": ["container-ops", "read-only"]}
# TOKEN_SCOPES='{"user-token": ["container-ops"], "admin-token": ["admin"]}'

# MCP protocol version (default: 2024-11-05)
# MCP_PROTOCOL_VERSION=2024-11-05

# Tool execution timeout in seconds (default: 30)
# MCP_TOOL_TIMEOUT=30

# Enforce output schema validation (default: false)
# ENFORCE_OUTPUT_SCHEMA=false

# Strict context size limit enforcement (default: false)
# STRICT_CONTEXT_LIMIT=false

# Transport mode: http (default) or sse
MCP_TRANSPORT=http

# Logging Configuration
# Options: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# CORS Configuration
# Comma-separated list of allowed origins
# WARNING: "*" is INSECURE for production - use only for local development
# Example: https://example.com,https://app.example.com
ALLOWED_ORIGINS=*

# Optional: Filter Configuration File Path
# FILTER_CONFIG_PATH=filter-config.json

# Intent Classification Configuration
# Enable automatic task-type detection from natural language queries (default: true)
# INTENT_CLASSIFICATION_ENABLED=true

# Fallback behavior when no task types detected (default: true)
# true: Return all tools | false: Return error (strict mode)
# INTENT_FALLBACK_TO_ALL=true

# Minimum confidence threshold for accepting detected task types (0.0-1.0)
# Reserved for future LLM-based classification (default: 0.0)
# INTENT_MIN_CONFIDENCE=0.0

# Tool Discovery: Supports both explicit task_type and natural language query
# - Explicit: {"method": "tools/list", "params": {"task_type": "container-ops"}}
# - Intent-based: {"method": "tools/list", "params": {"query": "show me running containers"}}
